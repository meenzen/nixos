#!/usr/bin/env bash

set -eo pipefail

source "$(dirname "$0")/lib.sh"

print "=> Starting Install"

print "==> Formatting Code"
alejandra . >/dev/null 2>&1

print "==> Rebuilding NixOS"
git add .
sudo nixos-rebuild boot --upgrade --verbose --flake . $@
