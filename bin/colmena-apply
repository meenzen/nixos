#!/usr/bin/env bash

set -eo pipefail

source "$(dirname "$0")/lib.sh"

print "=> Starting Install"

print "==> Formatting Code"
alejandra . >/dev/null 2>&1

print "==> Adding Changes to Git"
git add .

print "==> Running 'colmena apply'"
colmena apply --verbose $@
